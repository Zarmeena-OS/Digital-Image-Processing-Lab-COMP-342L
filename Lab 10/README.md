# Lab 10 — Batch evaluation on dataset

Run CLAHE on all images in Content/ (and fallback dirs), save to `outputs/`.

## Run

```bash
cd "Lab 10"
python batch_eval.py
```

## Output

- **`Lab 10/outputs/<name>_clahe.png`** — One enhanced image per input.

## Files

- `batch_eval.py` — main script; `Content/` — put images here.
